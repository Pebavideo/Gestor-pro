node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Cache local do Firebase CLI
.firebase/
# Replit agent internal state (not project code)
.local/

# Local environment secrets (Firebase credentials etc.) - never commit
.env
.env.local
*.env

# Firebase service account keys, if ever downloaded into the repo
*firebase-adminsdk*.json
serviceAccountKey.json
